import {
  lineHeight,
  fontSize,
  borderRadius,
  borderWidth,
  opacity,
  shadow,
  space,
  fontFamily,
  fontWeight,
  color,
} from "styled-system";

const basicProps = [
  lineHeight,
  fontSize,
  borderRadius,
  borderWidth,
  opacity,
  shadow,
  space,
  fontFamily,
  fontWeight,
  color,
];

export { basicProps };
